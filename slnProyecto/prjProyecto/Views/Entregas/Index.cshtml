@model IEnumerable<prjProyecto.Models.Entrega>

@{
    ViewBag.Title = "Index";
}

<h2 style="color:aliceblue">Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th style="color:aliceblue">
            @Html.DisplayNameFor(model => model.Proveedor.ProveedorRUC)
        </th>
        <th style="color:aliceblue">
            @Html.DisplayNameFor(model => model.EntregaFecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td style="color:aliceblue">
            @Html.DisplayFor(modelItem => item.Proveedor.ProveedorRUC)
        </td>
        <td style="color:aliceblue">
            @Html.DisplayFor(modelItem => item.EntregaFecha)
        </td>
        <td style="color:aliceblue">
            @Html.ActionLink("Edit", "Edit", new { id=item.EntregaId }, new { @class = "text-danger" }) |
            @Html.ActionLink("Details", "Details", new { id=item.EntregaId }, new { @class = "text-danger" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EntregaId }, new { @class = "text-danger" })
        </td>
    </tr>
}

</table>
