@model IEnumerable<prjProyecto.Models.EntregaDetalle>

@{
    ViewBag.Title = "Index";
}

<h2 style="color:aliceblue">Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th style="color:aliceblue">
            @Html.DisplayNameFor(model => model.Entregas.EntregaId)
        </th>
        <th style="color:aliceblue">
            @Html.DisplayNameFor(model => model.Productos.ProductoDescripcion)
        </th>
        <th style="color:aliceblue">
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th style="color:aliceblue">
            @Html.DisplayNameFor(model => model.PrecioUnitario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td style="color:aliceblue">
            @Html.DisplayFor(modelItem => item.Entregas.EntregaId)
        </td>
        <td style="color:aliceblue">
            @Html.DisplayFor(modelItem => item.Productos.ProductoDescripcion)
        </td>
        <td style="color:aliceblue">
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td style="color:aliceblue">
            @Html.DisplayFor(modelItem => item.PrecioUnitario)
        </td>
        <td >
            @Html.ActionLink("Edit", "Edit", new { id=item.EntregaDetalleId }, new { @class = "text-danger" }) |
            @Html.ActionLink("Details", "Details", new { id=item.EntregaDetalleId }, new { @class = "text-danger" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EntregaDetalleId }, new { @class = "text-danger" })
        </td>
    </tr>
}

</table>
