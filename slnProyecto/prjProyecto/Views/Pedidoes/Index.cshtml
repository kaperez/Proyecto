@model IEnumerable<prjProyecto.Models.Pedido>

@{
    ViewBag.Title = "Index";
}

<h2 style="color:aliceblue">Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th style="color:aliceblue">
            @Html.DisplayNameFor(model => model.Cliente.ClienteRUC)
        </th>
        <th style="color:aliceblue">
            @Html.DisplayNameFor(model => model.PedidoFecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td style="color:aliceblue">
            @Html.DisplayFor(modelItem => item.Cliente.ClienteRUC)
        </td>
        <td style="color:aliceblue">
            @Html.DisplayFor(modelItem => item.PedidoFecha)
        </td>
        <td style="color:aliceblue">
            @Html.ActionLink("Edit", "Edit", new { id=item.PedidoId }, new { @class = "text-danger" }) |
            @Html.ActionLink("Details", "Details", new { id=item.PedidoId }, new { @class = "text-danger" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PedidoId }, new { @class = "text-danger" })
        </td>
    </tr>
}

</table>
